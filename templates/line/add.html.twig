{% extends 'base.html.twig' %}

{% block title %}Ajouter une ligne de métro{% endblock %}

{% block body %}
    <div class="container mt-4">
        <div class="card-header text-center ">
                <h1 class="mb-0">Ajout d'une ligne de métro</h1>
        </div>
        <div class="card-body row d-flex text-center">
            <div class="col-md-6 ">
                {{ form_start(addLineForm) }}
                <div class="mb-3 ">
                    {{ form_row(addLineForm.lineFile) }}
                </div>
                <div class="mb-3">
                    {{ form_row(addLineForm.saveButton) }}
                </div>
                <div class="mb-3">
            </div>
                    {{ form_end(addLineForm) }}
        </div>
                        
    </div>

{% endblock %}
