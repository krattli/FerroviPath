{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    <div class="container mt-4 d-flex flex-column align-items-center w-50">
        <div class="card-header text-center ">
                <h1 class="mb-3"> Inscription</h1>
        </div>
        <div class="card shadow-sm w-100">
            <div class="card-body row d-flex justify-content-center text-center">
                <div class="col-md-8">
                    {{ form_start(registrationForm) }}
                        <div class="mb-3 ">
                            {{ form_row(registrationForm.pseudo) }}
                        </div>
                        <div class="mb-3">
                            {{ form_row(registrationForm.email) }}
                        </div>
                        <div class="mb-3">
                            {{ form_row(registrationForm.plainPassword, {
                                label: 'Mot de passe'
                            }) }}
                        </div>
                        <div class="mb-3">
                            {{ form_row(registrationForm.birth) }}
                        </div>
                </div>
            </div>
        </div>
        <div class="text-center">
        {{ form_row(registrationForm.save) }}
        {{ form_end(registrationForm) }}
        
        <p>Vous avez déjà un compte ? <a href="{{ path('ferrovipath_login') }}" class="link-primary mt-2">Connectez-vous</a></p>
        </div>
    </div>

{% endblock %}