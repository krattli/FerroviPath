{% extends 'base.html.twig' %}

{% block title %} Ferrovipath - Classement {% endblock %}

{% block body %}
    <div class="container mt-4">
        <h2 class="mb-4">
            <i class="fas fa-list" aria-hidden="true"></i> Classement par ligne
        </h2>

        <form method="get" action="{{ path('ferrovipath_rank') }}" class="mb-4">
            <label for="line">Choisissez une ligne :</label>
            <select name="line" id="line" class="form-select w-auto d-inline-block mx-2">
                <option value="">-- Choisir une ligne --</option>
                {% for line in lines %}
                    <option value="{{ line.idLine }}" {% if selectedLineId == line.idLine %}selected{% endif %}>
                        {{ line.getNameLine }}
                    </option>
                {% endfor %}
            </select>
            <button type="submit" class="btn btn-primary">Voir classement</button>
        </form>

        {% if joueurs is empty %}
            <p class="alert alert-warning">Aucun utilisateur n’a encore joué pour cette ligne.</p>
        {% else %}
            <table class="table table-bordered table-hover table-striped">
                <thead class="table-dark">
                <tr>
                    <th>Position</th>
                    <th>Pseudo</th>
                    <th>Score total</th>
                </tr>
                </thead>
                <tbody>
                {% for utilisateur in joueurs %}
                    <tr>
                        <td>{{ loop.index }}</td>
                        <td>{{ utilisateur.pseudo }}</td>
                        <td>{{ utilisateur.totalScore }}</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        {% endif %}
    </div>
{% endblock %}
